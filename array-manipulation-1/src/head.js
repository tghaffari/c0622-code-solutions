/* exported head */
// Define a function
// Use index 0 to set the value of a new variable to the first element of the array
// return the value from the function

function head(array) {
  var firstElement = array[0];
  return firstElement;
}
