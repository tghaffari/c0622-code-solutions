/* exported last */
// Define the function.
// Find the index of the last element by subtracting one from the length of the array
// and assign that element to a new variable
// Return the value of the new variable from the function

function last(array) {
  var lastElement = array[array.length - 1];
  return lastElement;
}
