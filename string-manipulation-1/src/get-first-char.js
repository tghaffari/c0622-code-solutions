/* exported getFirstChar */
// Define the function passing through the variable string
// Declare a variable that will hold the first character of the string
//    the first character can be found at the 0 index of the string
// Return the variable holding the value of the first character from the function

function getFirstChar(string) {
  var firstChar = string[0];
  return firstChar;
}
