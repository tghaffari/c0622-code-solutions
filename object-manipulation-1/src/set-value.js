/* exported setValue */
// Declare a function.
// using bracket notation, assign the value of the key to an object (key is the
// property).

function setValue(object, key, value) {
  object[key] = value;
}
