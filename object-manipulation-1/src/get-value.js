/* exported getValue */
// Declare a function.
// Declare a new variable to add the value of the desired object property using
// bracket notation.
// Return the variable from the function.

function getValue(object, key) {
  var value = object[key];
  return value;
}
