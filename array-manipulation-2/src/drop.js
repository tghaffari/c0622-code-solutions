/* exported drop */
// Declare a function
// Declare a variable to hold the new array
// Slice the array at the designated count
// Return the new array from the function

function drop(array, count) {
  var newArray = array.slice(count);
  return newArray;
}
