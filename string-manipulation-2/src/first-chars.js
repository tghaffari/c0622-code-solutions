/* exported firstChars */
// Declare a function
// Declare a variable to hold your new string
// Slice the word at the designated length
// Return the value from the function

function firstChars(length, string) {
  var cutString = string.slice(0, length);
  return cutString;
}
